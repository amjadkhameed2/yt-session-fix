# yt-session-generator (Railway Fix)

Patches the `no_sandbox` issue when running as root on Railway.

## Deploy
1. Connect this repo to Railway
2. Railway will build using the Dockerfile automatically
3. Done — no environment variables needed
